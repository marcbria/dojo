dojo:
  id: journalName
  tool: ojs
  version: "stable-3_3_0"
  versionFamily: "3_3_0"
  domain: "revista02.precarietat.net"

proxy:
  domains: "`revista02.precarietat.net`,`journalName`"

database:
  tool: mariadb
  version: "10.2"
  host: db
  user: ojs
  name: ojs
  driver: mysqli

images:
  db: "{{ database.tool }}:{{ database.version }}"
  app: "pkpofficial/{{ dojo.tool }}:{{ dojo.version }}"

pkp:
  general:
    installed: On
    base_url: "https://{{ dojo.domain }}"
    scheduled_tasks: On
  database:
    host:   "{{ database.host }}"
    user:   "{{ database.user }}"
    driver: "{{ database.driver }}"
  cache:
  i18n:
  files:
  security:
  email:
  search:
  interface:
  captcha:
  cli:
  proxy:
  debug:
  curl:
