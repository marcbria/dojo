; <?php exit(); // DO NOT DELETE ?>
; DO NOT DELETE THE ABOVE LINE!!!
; Autogenerated for {{ dojo.id }}
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

[general]
installed = "{{ config.installed | default('Off') }}"
base_url = "{{ config.base_url | default('http://pkp.sfu.ca/ojs') }}"
time_zone = "{{ config.time_zone | default('UTC') }}"

[database]
driver = "{{ config.database.driver | default('mysqli') }}"
host = "{{ config.database.host | default('localhost') }}"
username = "{{ config.database.username | default('ojs') }}"
password = "{{ config.database.password | default('ojs') }}"
name = "{{ config.database.name | default('ojs') }}"
debug = "{{ config.database.debug | default('Off') }}"
