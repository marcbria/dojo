#########################################################################
#
# File: .env
#
# Site:      {{ service }}
# Tool:      {{ allVars.dojo.tool }}
# Version:   {{ allVars.dojo.version }}
# Creation:  {{ fileCreation }}
#
#########################################################################

COMPOSE_PROJECT_NAME={{ allVars.dojo.id | lower }}
PROJECT_DOMAIN={{ allVars.dojo.domain }}
SERVERNAME=${PROJECT_DOMAIN}


### DATABASE ############################################################
DB_TYPE=mysql                           # Database Type to use. mysql or pgsql
DB_HOST=db                              # Database server hostname
# DB_PORT=                              # Database server port
# DB_SOCK=                              # Database unix socket instead of host/port
DB_NAME=lime                            # Database name
DB_TABLE_PREFIX="lime_"                 # Database table prefix; set this to a single whitespace if you don't want a table prefix.
# DB_MYSQL_ENGINE= MySQL engine used for survey tables (values: MyISAM, InnoDB, default: MyISAM)
DB_USERNAME=lime                        # Database user
DB_PASSWORD=limePwd                     # Database user's password
ADMIN_USER=Admin                        # Initial LimeSurvey Admin User
ADMIN_NAME=admin                        # Initial LimeSurvey Admin Username
ADMIN_EMAIL=marc.bria@uab.cat           # Initial LimeSurvey Admin Email
ADMIN_PASSWORD=limePwd                  # Initial LimeSurvey Admin Password
PUBLIC_URL="https://ada-survey.precarietat.net/"   # Public URL for public scripts
BASE_URL="https://ada-survey.precarietat.net/"     # Application Base URL
# URL_FORMAT=                           # URL Format. path or get
# TABLE_SESSION= Enable table sessions (true)
# SHOW_SCRIPT_NAME= Script name in URL (true|false). Default: true
# DEBUG= Debug level (0, 1, 2). Default: 0
# DEBUG_SQL= SQL Debug level (0, 1, 2). Default 0
# ENCRYPT_KEYPAIR= Data encryption keypair
# ENCRYPT_PUBLIC_KEY= Data encryption public key
# ENCRYPT_SECRET_KEY= Data encryption secret key
# ENCRYPT_NONCE= Data encryption nonce (used in 5.0 and higher)
# ENCRYPT_SECRET_BOX_KEY= Data encryption secret box key (used in 5.0 and higher)
# LISTEN_PORT= Apache: Listen port. Default: 8080

# Remember this to fill your installation form in a clean installation
# MYSQL_ROOT_PASSWORD={{ vault.general.MYSQL_ROOT_PASSWORD | default('limePwd') }}
# MYSQL_USER={{ allVars.database.user | default('lime') }}
# MYSQL_PASSWORD={{ vault.general.MYSQL_PASSWORD | default('limePwd') }}
# MYSQL_DATABASE={{ allVars.database.name | default('lime') }}

# ### LIMESURVEY ##########################################################
# LIMESURVEY_DB_HOST={{ allVars.database.host | default('db') }}
# LIMESURVEY_DB_USER={{ allVars.database.user | default('lime') }}
# LIMESURVEY_DB_PASSWORD={{ vault.general.MYSQL_PASSWORD | default('limePwd') }}
# LIMESURVEY_DB_NAME={{ allVars.database.name | default('lime') }}
# LIMESURVEY_TABLE_PREFIX={{ allVars.database.prefix | default('lime_') }}
# LIMESURVEY_ADMIN_USER="admin"
# LIMESURVEY_ADMIN_PASSWORD={{ allVars.app.passwd | default('limePwd') }}
# LIMESURVEY_ADMIN_NAME="Survey Administrator"
# LIMESURVEY_ADMIN_EMAIL={{ allVars.app.mail | default('marc.bria@uab.cat') }}
# LIMESURVEY_DEBUG=0
# LIMESURVEY_SQL_DEBUG=0
# LIMESURVEY_USE_INNODB=""
# LIMESURVEY_USE_DB_SESSIONS=""