#########################################################################
# An override for traefik – adds proxy network
#########################################################################

services:
  app:
    networks:
      - {{ allVars.app.network | default('proxy') }}

networks:
  {{ allVars.app.network | default('proxy') }}:
    external: true
